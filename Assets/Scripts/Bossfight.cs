using UnityEngine;

public class Bossfigth : MonoBehaviour
{
    // Напиши боссфайт с большим боссом от которого надо уворачиваться и боевую механику, когда раз в 3 секунды можно выстрелить объектом и нанести ему урон, и сверху будет полоса с хп босса на Юнити
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
